export const metadata = {
    title: "Admin Dashboard — Digital Life Lessons",
};

const AdminDashboardHome = () => {
    return (
        <div>
            <p className="text-xs text-dll-muted">Dashboard</p>
            <h1 className="font-serif text-xl font-semibold text-dll-heading mb-2">Admin Dashboard</h1>
            <p className="text-sm text-dll-muted">Analytics, manage users, manage lessons, and reported lessons are coming soon.</p>
        </div>
    );
};

export default AdminDashboardHome;
